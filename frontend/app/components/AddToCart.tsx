'use client';

import React from 'react'

const AddToCart = () => {
  return (
    <div>
      <button onClick={() => console.log("Hello world!")}>Add</button>
    </div>
  )
}

export default AddToCart
